page << <<-EOS
  $$('#story_list ##{dom_id(@story)}').each(function(element){
    element.fade()
    element.removeClassName('stripe')
  })
  $$('##{dom_id(@story, 'release')}').each(function(element){
    element.update('#{link_to @story.release.description, release_stories_path(@story.release)}')
  })
  restripe('story_list')
EOS
rjs_update_estimate_total page, @previous_release